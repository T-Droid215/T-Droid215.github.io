const introText = "Welcome to the empire...\n> Assembling the greeters...\n> Gates opening!";
let i = 0;
const speed = 50;

function typeEffect() {
    if (i < introText.length) {
        let char = introText.charAt(i);
    }
}

document.addEventListener("DOMContentLoeaded", typeEffect);